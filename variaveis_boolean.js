/*Para continuar, defina duas variáveis: umNumeroPequeno e umNumeroGrande, e atribua a elas valores 
numéricos diferentes de acordo com seus nomes. Então defina a variável eMenor e atribua o resultado da
 comparação se umNumeroPequeno é menor do que umNumeroGrande; e defina a variável eMaior, com o resultado
  da comparação umNumeroPequeno é maior que aNumeroGrande.
*/


var umNumeroPequeno = 10;
var umNumeroGrande = 20;

var eMenor = umNumeroPequeno < umNumeroGrande;
var eMaior = umNumeroPequeno > umNumeroGrande;

console.log("umNumeroPequeno é menor do que umNumeroGrande: " + eMenor);
console.log("umNumeroPequeno é maior que aNumeroGrande: "+ eMaior);